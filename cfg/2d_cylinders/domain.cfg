# 2D random cylinder pack (RSA). radius/distance set the pore scale ell.
# cylinders (2D, periodic z) RSA geometry for A_Bo0.15
# target porosity ~ 0.3 -> max_number ~ 713  (RSA jams earlier if distance is large; tune both)
radius       = 16
max_number   = 30713
distance     = 6     # minimum gap between obstacle centres (RSA)
seed         = 12345
is_hele_shaw = false      # false -> periodic in z (a stack of 2D arrays => quasi-2D)
